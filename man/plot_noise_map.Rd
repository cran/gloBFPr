% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/noise.R
\name{plot_noise_map}
\alias{plot_noise_map}
\title{Plot a NoiseModelling-style road-noise map}
\usage{
plot_noise_map(
  x,
  period = "DEN",
  field = "LAEQ",
  resolution = NULL,
  breaks = c(35, 40, 45, 50, 55, 60, 65, 70, 75),
  nodata = -99,
  palette = noise_map_palette(),
  road_col = "white",
  road_alpha = 0.35,
  road_lwd = 1.4,
  building_col = "black",
  legend = TRUE,
  legend_width = 0.26,
  legend_cex = 0.85,
  scalebar = TRUE,
  scalebar_unit = c("auto", "km", "m"),
  scalebar_cex = 0.7,
  north_arrow = TRUE,
  mar = c(0.2, 0.2, 0.2, 0.2),
  add = FALSE,
  ...
)
}
\arguments{
\item{x}{A result from \code{get_noise_map(run = TRUE)}, a \code{gloBFPr_noise_surface}
object, or an \code{sf} receiver noise map.}

\item{period}{Noise period to plot. Defaults to \code{"DEN"}.}

\item{field}{Noise field to plot. Defaults to \code{"LAEQ"}.}

\item{resolution}{Optional raster resolution in map units when \code{x} is an
\code{sf} receiver map.}

\item{breaks}{Noise class breakpoints in dB.}

\item{nodata}{Values at or below this threshold are treated as no-data.
Defaults to \code{-99}, NoiseModelling's no-result sentinel value.}

\item{palette}{Fill colors from quiet to loud.}

\item{road_col}{Road overlay color. Defaults to white with transparency.
Use \code{NA} to omit roads.}

\item{road_alpha}{Road overlay alpha from \code{0} fully transparent to \code{1}
opaque.}

\item{road_lwd}{Road overlay line width.}

\item{building_col}{Building fill color. Use \code{NA} to omit buildings.}

\item{legend}{Logical. Draw the dB(A) class legend in a dedicated panel to
the right of the map. Defaults to \code{TRUE}.}

\item{legend_width}{Width of the legend panel relative to the map panel.
Smaller values give the map more room. Defaults to \code{0.26}.}

\item{legend_cex}{Legend text size. Defaults to \code{0.85}.}

\item{scalebar}{Logical. Draw a distance scale bar just below the legend
(or in the bottom-left of the map when \code{legend = FALSE}). Defaults to
\code{TRUE}. Distances assume a projected CRS in metres; for geographic
coordinates they are approximated at the map's mid-latitude.}

\item{scalebar_unit}{Scale bar unit: \code{"auto"} (default), \code{"km"}, or \code{"m"}
to pick whichever keeps the label readable.}

\item{scalebar_cex}{Scale bar label size. Defaults to \code{0.7}.}

\item{north_arrow}{Logical. Draw a north arrow in the lower-left map area.
Defaults to \code{TRUE}.}

\item{mar}{Margins (in lines) around the map panel. Defaults to a tight
margin so the map fills the device.}

\item{add}{Logical. If \code{TRUE}, add to the current plot.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisibly returns the isosurface \code{sf} object or fallback
\code{gloBFPr_noise_surface} object used for plotting.
}
\description{
Draws NoiseModelling isosurface polygons underneath roads and buildings when
available. If \code{x} does not contain official isosurfaces, it falls back to an
interpolated receiver surface.
}
